import React from 'react';

const Ridestat = (props) => {
    return (
        <div>
            <h4>{props.fullname}</h4>
            <h4>{props.ride_date}</h4>
        </div>

}

export default Ridestat;